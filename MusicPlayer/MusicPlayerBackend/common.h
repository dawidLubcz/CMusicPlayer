#ifndef COMMON
#define COMMON

#include <inttypes.h>
#include "logger.h"

typedef enum
{
    FALSE = 0,
    TRUE
}E_BOOL;

typedef enum
{
    ERR_OK = 0,
    ERR_NOK = -1
}E_ERROR_CODE;

#endif // COMMON

